# Concept Modules (L2)

One folder per concept (hashing ring, quorum, vector clocks, ...). Pure logic,
no rendering, plugs into the L1 engine.

**Hand-written boundary: humans write everything in this folder. Agents write
tests against it, never the logic itself.**
