package main

import "log"

func main() {
	// TODO(human): wire up a tiny CLI — `lsm set k v`, `lsm get k`, `lsm del k`,
	// plus a `lsm bench` mode that writes 10k keys for the crash-recovery test.
	log.Fatal("TODO(human): LSM engine not implemented yet — see doc.go for the spec")
}
